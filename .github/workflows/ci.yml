name: C CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Install Dependencies
      run: sudo apt-get update && sudo apt-get install -y gcc

    - name: Compile
      run: gcc -o test_math math.c test_math.c

    - name: Run Tests
      run: ./test_math